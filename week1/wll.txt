# lib
kyrio_meros() {    

    akeraios number1, number2, sum;
    
    grapse("Enter two integers: ");
    diabase(number1, number2);

    ...calculate the sum...
    sum = number1 + number2;      
    
    grapse(number1, number2, sum);
    epestrepse 0;
}
