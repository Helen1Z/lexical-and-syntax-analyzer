# lib
kyrio_meros() {   

    leksh str1,str2;
    akeraios i;
    gia(i=0;i<0;i=i+1){
        diabase(number);
        number=number+i;
    }

    pragmatikos num1, num2, num3, num4, num5, num6, num7;
    akeraios count;
    
    grapse("Enter two numbers: ");
    diabase(number1, number2);

    (*calculate the average*)
    sum = number1 + number2;      
    count=2;

    grapse(number1, number2, sum/count);
    epestrepse 0;
}
